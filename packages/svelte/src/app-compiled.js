/* App.svelte generated by Svelte v3.26.0 */
import {
	SvelteComponent,
	attr,
	create_component,
	destroy_component,
	detach,
	element,
	init,
	insert,
	mount_component,
	noop,
	safe_not_equal,
	transition_in,
	transition_out
} from "svelte/internal";

import HelloWorld from "./HelloWorld.svelte";

function create_fragment(ctx) {
	let main;
	let helloworld;
	let current;
	helloworld = new HelloWorld({ props: { name: "World" } });

	return {
		c() {
			main = element("main");
			create_component(helloworld.$$.fragment);
			attr(main, "class", "app svelte-1jrzh1z");
		},
		m(target, anchor) {
			insert(target, main, anchor);
			mount_component(helloworld, main, null);
			current = true;
		},
		p: noop,
		i(local) {
			if (current) return;
			transition_in(helloworld.$$.fragment, local);
			current = true;
		},
		o(local) {
			transition_out(helloworld.$$.fragment, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(main);
			destroy_component(helloworld);
		}
	};
}

class App extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, null, create_fragment, safe_not_equal, {});
	}
}

export default App;